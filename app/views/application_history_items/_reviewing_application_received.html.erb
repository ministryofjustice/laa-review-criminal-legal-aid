<strong>
  <% if event_is_for_current_version %>
    <%= t('reviewing.application_received', scope: 'event.description') %>
  <% else %>
    <%= link_to t('reviewing.application_received', scope: 'event.description'),
                crime_application_path(application.id) %>
  <% end %>
</strong>
