<strong>
  <%= t(application.submission_type, scope: 'event.description.assigning.reassigned_to_user', to_whom_name: User.name_for(item.event_data.fetch(:to_whom_id)), from_whom_name: User.name_for(item.event_data.fetch(:from_whom_id))) %>
</strong>
