<%= govuk_table do |table| %>
  <%= table.with_head do |head|
        head.with_row do |row|
          row.with_cell(text: thead_text(:processed_on), header: true)
          row.with_cell(text: thead_text(:applications_closed), numeric: true, header: true)
        end
      end %>
  <%= table.with_body do |body|
        report.rows.each do |data_row|
          body.with_row do |row|
            data_row.each do |data_cell|
              row.with_cell(
                text: data_cell.content,
                numeric: data_cell.numeric,
                header: data_cell.header
              )
            end
          end
        end
      end %>
<% end %>
